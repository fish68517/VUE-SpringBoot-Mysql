1.  **截图内容**: 您提供的截图 `image_59108a.png` 展示了入库管理界面的表格，其中一行入库单 `IN-20251011-001` 被展开，显示了其关联的入库批次详情（批次号、产品SKU、产品名称、计划数量）。展开区域的左下角有一个 "生成/打印二维码" 按钮。右上角有 "手动创建入库单"、"从Excel导入" 和 "下载模板" 按钮。
2.  **当前下载功能**: 截图右上角有一个 "下载模板" 按钮，它的功能是下载用于 Excel 导入的模板文件。
3.  **您的需求**: 您希望增加一个新的下载功能，具体要求是：
    * **位置**: 在**每一笔**入库订单旁边（或操作列中）设置一个下载按钮。
    * **内容**: 下载的文件需要包含该笔入库订单关联的**入库批次详情**（批次号、产品SKU、产品名称、计划数量等）。

**设计方案:**

我们将在入库单表格的**操作列**中为每一行添加一个 "下载详情" 按钮。点击该按钮后，将触发一个函数，该函数会获取当前行入库单关联的批次详情数据，并将其整理成 CSV 或 Excel 格式进行下载。

---