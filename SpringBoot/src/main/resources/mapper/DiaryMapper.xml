<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.admin.mapper.DiaryMapper">

    <resultMap id="DiaryResultMap" type="com.admin.entity.Diary">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="mood" property="mood"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.admin.entity.Diary" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO diaries (user_id, content, mood, created_at, updated_at)
        VALUES (#{userId}, #{content}, #{mood}, NOW(), NOW())
    </insert>

    <select id="findByUserId" parameterType="Long" resultMap="DiaryResultMap">
        SELECT id, user_id, content, mood, created_at, updated_at
        FROM diaries
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
