<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.admin.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.admin.entity.Comment">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findById" parameterType="Long" resultMap="CommentResultMap">
        SELECT id, post_id, user_id, content, created_at, updated_at
        FROM comments
        WHERE id = #{id}
    </select>

    <select id="findByPostId" parameterType="Long" resultMap="CommentResultMap">
        SELECT id, post_id, user_id, content, created_at, updated_at
        FROM comments
        WHERE post_id = #{postId}
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.admin.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (post_id, user_id, content, created_at, updated_at)
        VALUES (#{postId}, #{userId}, #{content}, NOW(), NOW())
    </insert>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM comments
        WHERE id = #{id}
    </delete>

    <delete id="deleteByPostId" parameterType="Long">
        DELETE FROM comments
        WHERE post_id = #{postId}
    </delete>

</mapper>
