<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.mapper.HabitCheckinMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.graduation.entity.HabitCheckin">
        <id column="habit_checkin_id" property="habitCheckinId" />
        <result column="habit_track_id" property="habitTrackId" />
        <result column="checkin_date" property="checkinDate" />
        <result column="checkin_timestamp" property="checkinTimestamp" />
        <result column="checkin_status_enum" property="checkinStatusEnum" />
        <result column="checkin_note_text" property="checkinNoteText" />
        <result column="checkin_streak_count" property="checkinStreakCount" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        habit_checkin_id, habit_track_id, checkin_date, checkin_timestamp, checkin_status_enum, checkin_note_text, checkin_streak_count
    </sql>

</mapper>
