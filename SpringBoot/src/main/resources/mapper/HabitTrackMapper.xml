<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.mapper.HabitTrackMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.graduation.entity.HabitTrack">
        <id column="habit_track_id" property="habitTrackId" />
        <result column="campus_user_id" property="campusUserId" />
        <result column="habit_name_text" property="habitNameText" />
        <result column="habit_frequency_enum" property="habitFrequencyEnum" />
        <result column="habit_reminder_time" property="habitReminderTime" />
        <result column="habit_status_enum" property="habitStatusEnum" />
        <result column="habit_create_timestamp" property="habitCreateTimestamp" />
        <result column="habit_streak_count" property="habitStreakCount" />
        <result column="habit_total_count" property="habitTotalCount" />
        <result column="habit_goal_count" property="habitGoalCount" />
        <result column="habit_color_hex" property="habitColorHex" />
        <result column="habit_icon_code" property="habitIconCode" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        habit_track_id, campus_user_id, habit_name_text, habit_frequency_enum, habit_reminder_time, habit_status_enum, habit_create_timestamp, habit_streak_count, habit_total_count, habit_goal_count, habit_color_hex, habit_icon_code
    </sql>

</mapper>
