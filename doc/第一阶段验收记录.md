# 第一阶段验收记录

验证日期：2026-09-20  
范围：本地源代码、H5发行物、App-vue编译资源及浏览器操作  
结论：第一阶段代码与本地H5验证已完成；Android真机基线仍待设备验证；uniCloud上传按用户要求由用户自行执行。

## 1. 第一阶段工作包对应

| 工作包 | 已完成内容 | 验证状态 |
| --- | --- | --- |
| A1 工程与契约 | uni-app Vue3/TypeScript工程，根目录入口、24个页面、双主题、脚本及锁文件 | 类型、数据、路由检查通过 |
| A2 双端最小验证 | 登录、地图、图表、详情及本地存储；App-vue通过renderjs适配视图层 | H5运行及App资源编译通过，Android真机未验证 |
| A3 统一数据层 | JSON种子、三级角色、权限、状态机、本地保存、模拟时钟和场景重置 | 17项自动测试通过 |
| A4 大屏与地图 | 深色看板、区域设施、管线、压力曲线、指标联查、缩放/平移/筛选/点线选择 | 桌面1920与1366、手机390宽度浏览器检查通过 |
| A5 浅色业务主线 | 工作门户、通知动态通讯录、告警、四类工单、巡检基础任务、DMA、设备曲线和用水/压力报表 | 告警闭环、巡检上报、导出、角色与导航浏览器验证通过 |
| A6 阶段集成 | 页面跳转、错误ID空态、筛选返回、登录回跳、H5产物和交接文档 | 本地通过；线上托管不在本轮执行范围 |

## 2. 自动检查

### 类型、数据与路由

命令：`npm run check`。结果通过。

已检查 3 个场景、120 个设施、160 条管线、30 条告警、40 条初始工单、24 个巡检任务、30 个设备及 24 个注册页面。检查初始数据、外键、角色、坐标、采样间隔、压力样本时间顺序、页面文件与静态跳转目标。

证据：`code/unpackage/evidence/check.txt`。

### 业务测试

命令：`npm test`。结果为 **17 项通过、0 项失败**。

覆盖登录失败、访客禁写、操作员区域/执行人隔离、派单限制、告警工单完整状态迁移、验收驳回、重复创建幂等、保存失败回滚、刷新恢复、版本不匹配保护、场景重置、巡检必填和异常工单双向关联、计量公式、模拟时钟、CSV转义与登录回跳白名单。

证据：`code/unpackage/evidence/unit-tests.txt`。

## 3. 浏览器验收

命令：先执行 `npm run preview`，再执行 `npm run test:browser`。使用本机 Chrome `153.0.8010.50`，由 Playwright 进行实际页面操作。

最终结果：**15 组浏览器验收通过**；另记录一项 22 个业务页面逐页检查数量，加上登录与大屏共覆盖 24 个页面。

| 项目 | 实测结果 |
| --- | --- |
| 管理员登录、大屏地图和ECharts | 地图和图表均有实际Canvas渲染 |
| 地图点选、缩放后点选 | 点选更新设施详情，缩放后仍能正确选中 |
| 告警实际派单 | `ALM-001`生成关联工单并转为已受理；生成短信/邮件模拟记录，明确不是真实送达 |
| 操作员接单、处理和提交 | 使用操作员身份完成页面操作，不能执行验收 |
| 管理员验收和刷新 | 关联告警关闭，大屏23→22；刷新后保留 |
| 场景重置 | 恢复初始待处理告警与工单数量 |
| 搜索、分页和返回 | 筛选后的列表进入详情，返回保留搜索条件与列表结果 |
| 页面覆盖 | 全部注册页可访问，详情与图表不白屏 |
| 同一路径切换对象 | 工单详情WO-001→WO-002时内容正确切换 |
| 巡检上报 | 界面填写3个检查项，异常项生成工单，任务可完成 |
| CSV导出 | 下载真实CSV文件，含中文表头及ALM-001记录 |
| 错误ID | 显示不存在或无权访问及返回入口 |
| 桌面与手机布局 | 1920×1080、1366×768、390×844；已测页面无整页横向溢出，表格可在自身容器横向滚动 |
| 访客和操作员权限 | 访客无写按钮；操作员不能查看其他区域设施详情 |
| 未登录直接打开详情 | 回到登录，登录后恢复原工单目标 |

本轮实际操作期间 **页面运行异常0、失败资源请求0、第三方资源请求0**。这是被测流程的证据，不代表所有未来新增页面或第三方库永久没有网络行为。

证据：`code/unpackage/evidence/browser-acceptance.json`、实际导出文件 `告警导出样例.csv`。

代表截图：

- `code/unpackage/evidence/dashboard-1920.png`
- `code/unpackage/evidence/alarms-1920.png`
- `code/unpackage/evidence/map-1920.png`
- `code/unpackage/evidence/workflow-closed.png`
- `code/unpackage/evidence/inspection-completed.png`
- `code/unpackage/evidence/dashboard-1366.png`
- `code/unpackage/evidence/alarms-1366.png`
- `code/unpackage/evidence/mobile-home-390.png`
- `code/unpackage/evidence/mobile-tasks-390.png`
- `code/unpackage/evidence/mobile-map-390.png`

开发过程中发现的地图初始化、图表异步加载、同路径详情参数缓存、未登录回跳覆盖及框架外部装饰图请求均已修复，并进入最终浏览器回归。

## 4. 构建结果

### H5

`npm run build:h5` 成功。最终本地发行检查通过，目录 `code/unpackage/dist/build/h5` 共 41 个文件，检查时总大小约 1.61 MB。准确值及首页校验值见 `code/unpackage/evidence/release-check.json`。

仅上传该H5目录内容即可进行后续托管发布，实际云端访问须在用户上传后验证。本轮未执行上传。

### App-vue

`npm run build:app` 成功，输出 `code/unpackage/dist/build/app`，包含 App 服务脚本、renderjs、视图资源和内置演示数据。证据：`code/unpackage/evidence/app-build.txt`。

产物为 **App资源，不是签名APK**。未生成或借用其他项目AppID、包名与签名。未调用Android云打包服务。

## 5. 尚未验收的事项

| 事项 | 原因/处理方式 |
| --- | --- |
| uniCloud线上访问、HTTPS与域名限制 | 用户明确自行上传，本轮不操作云端 |
| Android真机基线 | `adb devices`未发现设备；需后续连接设备运行，核对地图点选回传、保存、重启和返回键 |
| HBuilderX界面菜单发行 | 本轮通过版本锁定的CLI构建验证，没有把IDE菜单发行记为已测 |
| 最终签名APK | 需要本项目AppID、包名与签名，属于后续Android交付 |
| 30分钟连续运行及严格性能目标 | 方案安排在第二阶段，本轮未做对应正式验收 |
| 地图编辑/审核、框面查询、轨迹和完整分析等 | 仍按已通过方案在第二阶段实施 |

## 6. 本轮交接文档

`code/README.md`：启动、构建、目录与阶段边界。

`code/account/README.md`：演示账号与密码，集中在账号目录。

`doc/第一阶段使用与数据维护说明.md`：演示路径、JSON维护、权限和本机保存规则。

`doc/第一阶段本地构建与交接说明.md`：实际工具版本、发行目录、用户自行上传与Android验证边界。
